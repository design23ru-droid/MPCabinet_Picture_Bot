# Промпты для работы с API Wildberries

> Готовые промпты для Claude/ChatGPT при разработке интеграций с Wildberries API.
> Используйте вместе со справочником [README.md](./README.md)

---

## Оглавление

- [Как пользоваться](#как-пользоваться)
- [Товары](#-товары)
- [Цены](#-цены)
- [Заказы FBS](#-заказы-fbs)
- [Заказы DBS](#-заказы-dbs)
- [Поставки FBW](#-поставки-fbw)
- [Аналитика](#-аналитика)
- [Отзывы и вопросы](#-отзывы-и-вопросы)
- [Чат с покупателями](#-чат-с-покупателями)
- [Реклама](#-реклама)
- [Финансы](#-финансы)
- [Универсальные шаблоны](#-универсальные-шаблоны)

---

## Как пользоваться

### Формат промпта

Каждый промпт содержит:
1. **Задача** — что нужно сделать
2. **Контекст** — платформа, язык, особенности
3. **Методы API** — какие endpoints использовать
4. **Важно** — ограничения и подводные камни

### Адаптация промптов

Замените плейсхолдеры в квадратных скобках на ваши данные:
- `[ЯЗЫК]` — JavaScript, Python, Google Apps Script и т.д.
- `[subjectId]` — ID категории товара
- `[nmId]` — артикул WB (номенклатура)
- `[orderId]` — ID заказа
- `[supplyId]` — ID поставки

**Справочник методов:** [01-products.md](./01-products.md), [04-orders-fbs.md](./04-orders-fbs.md), и др.

---

## 📦 Товары

### Создать новый товар

```
Задача: Создать функцию для добавления нового товара на Wildberries.

Контекст:
- Язык: [ЯЗЫК]
- Нужно: получить характеристики категории, создать карточку, установить цену
- Категория (subjectId): [указать]

Данные товара:
- Название: [название]
- Артикул продавца: [vendorCode]
- Цена: [цена]
- Описание: [описание]
- Бренд: [бренд]

Методы API:
1. GET /content/v2/object/charcs/{subjectId} — характеристики категории
2. POST /content/v2/cards/upload — создание карточки
3. POST /api/v2/upload/task — установка цены

Важно:
- Лимит 100 карточек за запрос
- Баркод генерируется автоматически или через /content/v2/barcodes
- Base URL для контента: https://content-api.wildberries.ru
- Base URL для цен: https://discounts-prices-api.wildberries.ru
```

**Справочник:** [01-products.md](./01-products.md), [02-prices.md](./02-prices.md)

---

### Обновить карточку товара

```
Задача: Создать функцию для обновления существующей карточки товара.

Контекст:
- Язык: [ЯЗЫК]
- nmId товара: [указать]

Что обновить:
- [описание / характеристики / медиа / ...]

Методы API:
1. POST /content/v2/get/cards/list — получить текущие данные
2. POST /content/v2/cards/update — обновить карточку

Важно:
- При обновлении нужно передать ВСЕ поля карточки, включая те, что не меняются
- Нельзя обновлять: баркоды, photos, video, tags, цены
- Лимит 3000 номенклатур за запрос
```

**Справочник:** [01-products.md](./01-products.md)

---

### Загрузить фото/видео

```
Задача: Создать функцию для загрузки медиафайлов к карточке товара.

Контекст:
- Язык: [ЯЗЫК]
- nmId товара: [указать]
- Тип загрузки: [файл напрямую / по ссылке]

Методы API:
- POST /content/v3/media/file — загрузка файла (multipart/form-data)
- POST /content/v3/media/save — загрузка по ссылкам

Важно:
- Фото: до 10 штук, JPG/PNG, до 10 МБ
- Видео: 1 штука, MP4, до 50 МБ
- Порядок фото определяется порядком загрузки
```

**Справочник:** [01-products.md](./01-products.md)

---

### Получить список товаров с фильтрацией

```
Задача: Создать функцию для получения списка карточек товаров с пагинацией и фильтрами.

Контекст:
- Язык: [ЯЗЫК]
- Фильтры: [по бренду / по категории / по тегу / ...]

Методы API:
- POST /content/v2/get/cards/list

Параметры:
- cursor.limit — количество (макс. 100)
- cursor.updatedAt — курсор пагинации
- cursor.nmID — курсор пагинации
- filter.withPhoto — только с фото (-1/0/1)
- filter.tagIDs — фильтр по тегам
- filter.textSearch — поиск по тексту

Важно:
- Для пагинации использовать cursor из предыдущего ответа
- Сортировка по updatedAt DESC
```

**Справочник:** [01-products.md](./01-products.md)

---

## 💰 Цены

### Установить/обновить цены

```
Задача: Создать функцию для массового обновления цен на товары.

Контекст:
- Язык: [ЯЗЫК]
- Данные: массив {nmId, price, discount}

Методы API:
- POST /api/v2/upload/task — установка цен и скидок

Формат данных:
{
  "data": [
    {"nmID": 123, "price": 1000, "discount": 10}
  ]
}

Важно:
- Base URL: https://discounts-prices-api.wildberries.ru
- Цена в рублях, скидка в процентах
- Изменения применяются не мгновенно (до 15 минут)
- Лимит: 1000 товаров за запрос
```

**Справочник:** [02-prices.md](./02-prices.md)

---

### Участие в акции WB

```
Задача: Создать функции для работы с акциями Wildberries.

Контекст:
- Язык: [ЯЗЫК]

Методы API:
1. GET /api/v1/calendar/promotions — список доступных акций
2. GET /api/v1/calendar/promotions/details?promotionID={id} — детали акции
3. GET /api/v1/calendar/promotions/nomenclatures?promotionID={id} — товары для участия
4. POST /api/v1/calendar/promotions/upload — добавить товар в акцию

Важно:
- Base URL: https://advert-api.wildberries.ru
- Проверять сроки акции и условия участия
- Некоторые акции требуют минимальную скидку
```

**Справочник:** [09-promotion.md](./09-promotion.md)

---

## 📋 Заказы FBS

### Получить новые заказы

```
Задача: Создать функцию для получения новых сборочных заданий FBS.

Контекст:
- Язык: [ЯЗЫК]

Методы API:
- GET /api/v3/orders/new — новые заказы

Важно:
- Base URL: https://marketplace-api.wildberries.ru
- Заказы автоматически переходят в статус "На сборке" через 10 минут
- Рекомендуется опрашивать каждые 1-5 минут
```

**Справочник:** [04-orders-fbs.md](./04-orders-fbs.md)

---

### Обработать заказ (полный цикл)

```
Задача: Создать функцию для полной обработки FBS заказа от получения до отгрузки.

Контекст:
- Язык: [ЯЗЫК]

Методы API (последовательность):
1. GET /api/v3/orders/new — получить новые заказы
2. POST /api/marketplace/v3/orders/meta — получить метаданные (для маркировки)
3. PUT /api/v3/orders/{orderId}/meta/sgtin — добавить код маркировки (если требуется)
4. POST /api/v3/supplies — создать поставку
5. PATCH /api/marketplace/v3/supplies/{supplyId}/orders — добавить заказы в поставку
6. POST /api/v3/orders/stickers — получить стикеры для печати
7. PATCH /api/v3/supplies/{supplyId}/deliver — передать поставку в доставку

Важно:
- Маркировка обязательна для определённых категорий (обувь, одежда, духи и т.д.)
- Нельзя смешивать FBS и DBS заказы в одной поставке
```

**Справочник:** [04-orders-fbs.md](./04-orders-fbs.md)

---

### Создать поставку и получить стикеры

```
Задача: Создать функцию для формирования поставки и печати стикеров.

Контекст:
- Язык: [ЯЗЫК]
- ID заказов: [массив orderId]

Методы API:
1. POST /api/v3/supplies — создать поставку
   Body: {"name": "Поставка от [дата]"}
2. PATCH /api/marketplace/v3/supplies/{supplyId}/orders — добавить заказы
   Body: {"orders": [orderId1, orderId2, ...]}
3. POST /api/v3/orders/stickers — получить стикеры
   Body: {"orders": [orderId1, orderId2, ...]}

Формат стикеров:
- type: "svg" или "png"
- width/height: размер в пикселях

Важно:
- Максимум 100 заказов за раз при добавлении
- Стикеры в формате base64
```

**Справочник:** [04-orders-fbs.md](./04-orders-fbs.md)

---

## 🚚 Заказы DBS

### Обработать DBS заказ

```
Задача: Создать функцию для обработки заказа с доставкой продавцом (DBS).

Контекст:
- Язык: [ЯЗЫК]

Методы API (последовательность):
1. GET /api/v3/dbs/orders/new — получить новые заказы
2. POST /api/v3/dbs/orders/client — получить данные покупателя (адрес, телефон)
3. POST /api/marketplace/v3/dbs/orders/status/confirm — перевести на сборку
4. POST /api/marketplace/v3/dbs/orders/status/deliver — перевести в доставку
5. POST /api/marketplace/v3/dbs/orders/status/receive — подтвердить получение

Важно:
- Данные покупателя доступны только для DBS заказов
- При отказе использовать /status/reject
- Срок доставки согласно условиям магазина
```

**Справочник:** [05-orders-dbs.md](./05-orders-dbs.md)

---

## 📦 Поставки FBW

### Создать поставку на склад WB

```
Задача: Создать функцию для формирования поставки товаров на склад Wildberries.

Контекст:
- Язык: [ЯЗЫК]

Методы API:
1. POST /api/v1/acceptance/options — получить доступные опции приёмки
2. GET /api/v1/warehouses — список складов WB
3. GET /api/tariffs/v1/acceptance/coefficients — коэффициенты приёмки на 14 дней
4. POST /api/v1/supplies — создать/получить список поставок

Важно:
- Base URL: https://supplies-api.wildberries.ru
- Выбирать склад с минимальным коэффициентом
- Коэффициент влияет на стоимость приёмки
```

**Справочник:** [08-supplies-fbw.md](./08-supplies-fbw.md)

---

## 📊 Аналитика

### Получить статистику продаж

```
Задача: Создать функцию для получения статистики продаж за период.

Контекст:
- Язык: [ЯЗЫК]
- Период: [dateFrom] — [dateTo]

Методы API:
- GET /api/v1/supplier/sales — продажи и возвраты
  Параметры: dateFrom, flag (0 — все, 1 — только изменения)
- GET /api/v1/supplier/orders — заказы
- GET /api/v1/supplier/stocks — остатки на складах WB

Важно:
- Base URL: https://statistics-api.wildberries.ru
- Данные хранятся до 90 дней
- Лимит: 1 запрос/мин для некоторых методов
- Обновление каждые 30 минут
```

**Справочник:** [13-reports.md](./13-reports.md)

---

### Анализ воронки продаж

```
Задача: Создать функцию для анализа воронки продаж по карточкам товаров.

Контекст:
- Язык: [ЯЗЫК]
- nmIds: [массив артикулов]
- Период: [begin] — [end]

Методы API:
- POST /api/analytics/v3/sales-funnel/products — статистика за период
- POST /api/analytics/v3/sales-funnel/products/history — по дням/неделям

Данные включают:
- Просмотры карточки
- Добавления в корзину
- Заказы
- Выкупы
- Конверсии на каждом этапе

Важно:
- Base URL: https://seller-analytics-api.wildberries.ru
- Максимум 7 дней за один запрос
```

**Справочник:** [12-analytics.md](./12-analytics.md)

---

## 💬 Отзывы и вопросы

### Получить и ответить на отзывы

```
Задача: Создать функцию для работы с отзывами покупателей.

Контекст:
- Язык: [ЯЗЫК]

Методы API:
1. GET /api/v1/feedbacks — список отзывов
   Параметры: isAnswered (true/false), take, skip, nmId
2. POST /api/v1/feedbacks/answer — ответить на отзыв
   Body: {"id": "feedbackId", "text": "Текст ответа"}
3. PATCH /api/v1/feedbacks/answer — редактировать ответ
   Body: {"id": "feedbackId", "text": "Новый текст"}

Важно:
- Base URL: https://feedbacks-api.wildberries.ru
- Редактировать ответ можно только 1 раз в течение 60 дней
- Лимит: 3 запроса/сек
```

**Справочник:** [10-feedbacks.md](./10-feedbacks.md)

---

### Закрепить лучшие отзывы

```
Задача: Создать функцию для закрепления отзывов на карточке товара.

Контекст:
- Язык: [ЯЗЫК]
- nmId: [артикул товара]

Методы API:
1. GET /api/feedbacks/v1/pins?nmId={nmId} — текущие закреплённые
2. GET /api/feedbacks/v1/pins/limits — лимиты закрепления
3. POST /api/feedbacks/v1/pins — закрепить отзывы
   Body: {"feedbackIds": ["id1", "id2"]}
4. DELETE /api/feedbacks/v1/pins — открепить
   Body: {"feedbackIds": ["id1"]}

Важно:
- Максимум 3-5 закреплённых отзывов на карточку (зависит от тарифа)
- Закреплённые отзывы показываются первыми
```

**Справочник:** [10-feedbacks.md](./10-feedbacks.md)

---

## 💬 Чат с покупателями

### Работа с чатом

```
Задача: Создать функцию для работы с чатами покупателей.

Контекст:
- Язык: [ЯЗЫК]

Методы API:
1. GET /api/v1/seller/chats — список всех чатов
2. GET /api/v1/seller/events — события (новые сообщения)
   Параметры: next (курсор пагинации)
3. POST /api/v1/seller/message — отправить сообщение
   Body: {"chatId": "id", "message": "Текст"}
4. GET /api/v1/seller/download/{id} — скачать файл из сообщения

Важно:
- Base URL: https://buyer-chat-api.wildberries.ru
- Лимит: 10 запросов/10 сек
- Ответить нужно в течение 10 дней
- Чат инициируется только покупателем
```

**Справочник:** [11-chat.md](./11-chat.md)

---

## 📢 Реклама

### Создать рекламную кампанию

```
Задача: Создать функцию для запуска рекламной кампании на Wildberries.

Контекст:
- Язык: [ЯЗЫК]
- Тип кампании: [Поиск / Каталог / Карточка товара]
- nmIds: [массив артикулов]

Методы API:
1. POST /api/advert/v1/bids/min — получить минимальные ставки
2. POST /adv/v2/seacat/save-ad — создать кампанию
   Body: {
     "name": "Название",
     "subjectId": 123,
     "nms": [nmId1, nmId2],
     "bid_type": "unified" или "manual",
     "placement_types": ["search", "recommendations"]
   }
3. POST /adv/v1/budget/deposit — пополнить бюджет
4. GET /adv/v0/start?id={campaignId} — запустить

Важно:
- Base URL: https://advert-api.wildberries.ru
- bid_type: "unified" — единая ставка, "manual" — ручная
- Минимальный бюджет обычно от 500 рублей
```

**Справочник:** [09-promotion.md](./09-promotion.md)

---

### Получить статистику рекламы

```
Задача: Создать функцию для получения статистики рекламных кампаний.

Контекст:
- Язык: [ЯЗЫК]
- campaignId: [ID кампании]

Методы API:
- POST /adv/v2/fullstats — статистика кампаний
  Body: [{"id": campaignId, "dates": ["2024-01-01", "2024-01-07"]}]
- GET /adv/v3/fullstats — альтернативный метод (GET)
- GET /adv/v1/stat/words — статистика по ключевым словам

Данные включают:
- Показы, клики, CTR
- Расходы
- Заказы, выручка
- ДРР (доля рекламных расходов)

Важно:
- Данные обновляются с задержкой до 3 часов
```

**Справочник:** [09-promotion.md](./09-promotion.md)

---

## 💵 Финансы

### Получить баланс и отчёты

```
Задача: Создать функцию для получения финансовой информации.

Контекст:
- Язык: [ЯЗЫК]

Методы API:
1. GET /api/v1/account/balance — текущий баланс
2. GET /api/v5/supplier/reportDetailByPeriod — детальный отчёт
   Параметры: dateFrom, dateTo, rrdid (курсор)
3. GET /api/v1/documents/list — список документов
4. GET /api/v1/documents/download?id={docId} — скачать документ

Важно:
- Base URL: https://common-api.wildberries.ru
- Отчёт о реализации формируется еженедельно
- Лимит на баланс: 1 запрос/мин
```

**Справочник:** [14-finances.md](./14-finances.md)

---

### Получить тарифы и комиссии

```
Задача: Создать функцию для получения актуальных тарифов Wildberries.

Контекст:
- Язык: [ЯЗЫК]

Методы API:
- GET /api/v1/tariffs/commission — комиссия по категориям
- GET /api/v1/tariffs/box — тарифы на доставку и хранение (короба)
- GET /api/v1/tariffs/pallet — тарифы для монопаллет
- GET /api/tariffs/v1/acceptance/coefficients — коэффициенты приёмки
- GET /api/v1/tariffs/return — тарифы на возврат

Важно:
- Коэффициенты приёмки на 14 дней вперёд
- Выбирайте склады с минимальным коэффициентом
```

**Справочник:** [15-tariffs.md](./15-tariffs.md)

---

## 🔧 Универсальные шаблоны

### Базовый HTTP-клиент для WB API

```
Задача: Создать базовый HTTP-клиент для работы с Wildberries API.

Контекст:
- Язык: [ЯЗЫК]

Требования:
1. Авторизация через заголовок Authorization: Bearer {token}
2. Обработка rate limits (заголовки X-Ratelimit-Remaining, X-Ratelimit-Retry)
3. Автоматический retry при 429 ошибке
4. Логирование запросов и ответов
5. Обработка ошибок API

Base URLs:
- common-api.wildberries.ru
- content-api.wildberries.ru
- discounts-prices-api.wildberries.ru
- marketplace-api.wildberries.ru
- statistics-api.wildberries.ru
- seller-analytics-api.wildberries.ru
- advert-api.wildberries.ru
- feedbacks-api.wildberries.ru
- buyer-chat-api.wildberries.ru
- supplies-api.wildberries.ru
```

**Справочник:** [README.md](./README.md)

---

### Обработка ошибок WB API

```
Задача: Создать функцию для обработки типичных ошибок Wildberries API.

Контекст:
- Язык: [ЯЗЫК]

Типичные ошибки:
- 400 Bad Request — неверные параметры
- 401 Unauthorized — невалидный токен
- 403 Forbidden — нет доступа к методу (проверить категории токена)
- 404 Not Found — ресурс не найден
- 409 Conflict — конфликт (считается как 10 запросов!)
- 429 Too Many Requests — превышен лимит
- 500/502/503 — ошибка сервера (retry)

Заголовки для rate limit:
- X-Ratelimit-Remaining — осталось запросов
- X-Ratelimit-Retry — через сколько секунд повторить
```

---

### Пагинация результатов

```
Задача: Создать универсальную функцию для пагинации результатов WB API.

Контекст:
- Язык: [ЯЗЫК]

Типы пагинации в WB API:

1. Курсор (Content API):
   - cursor.updatedAt, cursor.nmID
   - Получать из ответа, передавать в следующий запрос

2. Offset (Prices API):
   - offset, limit
   - Увеличивать offset на limit

3. Next (Chat API):
   - next — токен из предыдущего ответа
   - Повторять пока totalEvents > 0

4. rrdid (Reports API):
   - rrdid — ID последней записи
   - Передавать в следующий запрос
```

---

## Примеры эффективных промптов для Claude

### Короткий промпт (когда контекст понятен)

```
Напиши функцию на [ЯЗЫК] для получения новых FBS заказов из WB API.
Токен передаётся как параметр. Обработай ошибки и rate limit.
```

### Детальный промпт (для сложных задач)

```
Задача: Автоматизировать обработку отзывов на Wildberries.

Контекст:
- Google Apps Script
- Данные сохранять в Google Sheets
- Запускать по триггеру каждый час

Требования:
1. Получить все неотвеченные отзывы
2. Сохранить в таблицу: дата, товар, оценка, текст, статус
3. Для отзывов с оценкой 1-3 отправить уведомление в Telegram
4. Логировать ошибки

Методы API:
- GET /api/v1/feedbacks?isAnswered=false

Особенности:
- Лимит 3 запроса/сек
- Base URL: https://feedbacks-api.wildberries.ru
```

---

> **Версия:** 2026-01
> **Справочник методов:** [README.md](./README.md)
